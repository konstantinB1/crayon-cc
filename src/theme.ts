import { createTheme } from "@mui/material";

export default createTheme({
    palette: {
        mode: "dark",
        background: {
            default: "#000",
        },
        primary: {
            main: "#ed684e",
        },
        secondary: {
            main: "#dc004e",
        },
    },
});
